# TODOS

* Junit Tests (Adrian?)
* 

### Bugfixing

* columns in der tabelle kann man verschieben (ich glaub ned dass das gewollt ist)
* automatik da dupliziert sich des true und false ins infinity
* <muss noch schaun>

## Adrian

**Bugfixing: **

* irgendwas funktioniert noch nicht mit dem einlesen von adminrechten vom user in die db

## Trixi

* ah wenn amal grundsätzlich alles fertig is dann machma einen zweiten controller/.fmxl die für unsere admin leute zuständig is
  * da gibts einmal das editierbar machen und das hinzufügen 

## Matthias

* ich hätt gern dass der search button grundsätzlich so amal eine funktion hat die im prinzip die table einfach nur aktualisiert (z.b. wenn ich in der db da was hinzufüg hätt ich gern dass man mit dem button des aktualisiert)(einfach so als wärs ein select * from ...; da gibts eh scho a methode)
* alle felder die fürs search verwendet werden sollen brauchen eine funktion bzw. auch eine methode schreiben die sich den spaß aus der db holt (tipp: entweder kreativ werden wie die lösung wird oder wenns a bissi a schaß is dann red ma alle drüber und entscheiden dann)

## Gebhard

* wir müssen noch in der UI brand auf (brand | model) ändern (in der db ist es schon geändert)
* username und password den textfeldern eine funktion geben (bitte gleich auch eine "methode" fürs hashen der pws) ja und dem login button auch eine funktion geben

